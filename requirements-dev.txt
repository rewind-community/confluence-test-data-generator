# Development dependencies
# The ruff pin below is the single source of truth for its version: lint.yml installs
# ruff using this file as a pip constraints file, so CI and local development cannot
# drift. Bump it here and CI follows automatically.
ruff==0.16.0
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=1.4.0
pytest-xdist>=3.8.0
responses>=0.26.2
aioresponses>=0.7.9
